usage: engram [-h]
              {init,health,smoke,rebuild-kg,decay,migrate,train,admin} ...

positional arguments:
  {init,health,smoke,rebuild-kg,decay,migrate,train,admin}
    init                Create Neo4j indexes and SQLite schemas
    health              Print component readiness
    smoke               Run an end-to-end ingest + query test
    rebuild-kg          Rebuild Neo4j from the filesystem (§2.3)
    decay               Run the daily decay pass (§10.3)
    migrate             Run pending schema migrations (§13.5)
    train               Training pipeline (§14)
    admin               Tenant administration (local)

options:
  -h, --help            show this help message and exit
