# Template — DO NOT commit real secrets. Use a secret manager:
#   - HashiCorp Vault Agent Injector (vault.hashicorp.com/*)
#   - External Secrets Operator → AWS Secrets Manager / GCP Secret Manager
#   - Sealed Secrets (Bitnami) for GitOps
#
# This template is a placeholder for the variable names the app expects.
apiVersion: v1
kind: Secret
metadata:
  name: engram-secrets
  namespace: engram
type: Opaque
stringData:
  ENGRAM_API_KEY: "REPLACE_WITH_REAL_VALUE"
  ENGRAM_ADMIN_KEY: "REPLACE_WITH_REAL_VALUE"
  OLLAMA_API_KEY: "REPLACE_WITH_REAL_VALUE"
  NEO4J_ADMIN_PASSWORD: "REPLACE_WITH_REAL_VALUE"
